# 往年基础示例

> **⚠️ 重要提示**
> 以下内容基于 2025 年参赛作品的失败代码整理而成，仅作为**教学讲解作用**。
> 代码中存在诸多问题，请批判性学习，重点理解**设计思路**和**通信协议**！

---

## 2. 步进电机 & 5. 电机

本节阐述智能物流小车底盘电机控制系统的设计与实现，包括直流电机驱动、编码器测速、PID 闭环控制及位置控制。

---

### 2.1 电机驱动原理

#### 2.1.1 H 桥驱动电路

直流电机转向控制采用 H 桥驱动拓扑，其电路结构如下：

```
         +Vcc
           |
      +----+----+
      |         |
    +-+-       +-+-
    |A|       |B|
    +-+-       +-+-
      |         |
      +----+----+
           |
         Motor
           |
      +----+----+
      |         |
    +-+-       +-+-
    |C|       |D|
    +-+-       +-+-
      |         |
      +----+----+
           |
          GND
```

**控制逻辑**：
| A | B | C | D | 电机状态 |
|---|---|---|---|----------|
| 1 | 0 | 0 | 1 | 正转 |
| 0 | 1 | 1 | 0 | 反转 |
| 1 | 1 | 0 | 0 | 制动 |
| 0 | 0 | 0 | 0 | 悬空 |

#### 2.1.2 PWM 调速原理

STM32 通过 TIM 输出 PWM（脉宽调制）信号控制 H 桥：

```c
// PWM 周期 = (PSC+1) × (ARR+1) / 时钟频率
// 例：PSC=83, ARR=999, 时钟=84MHz
// 周期 = 84 × 1000 / 84MHz = 1ms → 1kHz

void MotorA_SetSpeed(int speed)
{
    if (speed >= 0)  // 正转
    {
        __HAL_TIM_SET_COMPARE(&htim10, TIM_CHANNEL_1, speed);  // PWM 输出
        __HAL_TIM_SET_COMPARE(&htim11, TIM_CHANNEL_1, 0);       // 关闭
    }
    else  // 反转
    {
        __HAL_TIM_SET_COMPARE(&htim10, TIM_CHANNEL_1, 0);
        __HAL_TIM_SET_COMPARE(&htim11, TIM_CHANNEL_1, -speed);
    }
}
```

PWM 占空比与电机转速近似呈线性关系，占空比 = CCR / ARR × 100%。

---

### 2.2 编码器测速

#### 2.2.1 正交编码器原理

增量式编码器输出两相正交脉冲信号 A 和 B，相位差 90°：

- A 相超前 B 相 90° → 正转
- B 相超前 A 相 90° → 反转

STM32 TIM 编码器模式自动识别方向并计数：

```c
void Encoder_Init(Encoder_t *encoder, TIM_HandleTypeDef *htim)
{
    encoder->htim = htim;
    encoder->Last_counter = 0;
    encoder->Total_counter = 0;
    encoder->Velocity = 0.0f;

    HAL_TIM_Encoder_Start(htim, TIM_CHANNEL_ALL);
    __HAL_TIM_SET_COUNTER(htim, 0);
}
```

#### 2.2.2 速度计算

```c
// 速度转换系数
// RPM = 脉冲增量 × 60秒 × 采样频率 / 分辨率
static const float VELOCITY_COEFFICIENT = (60.0f * CONTROL_FREQUENCY) / ENCODER_RESOLUTION;

void Encoder_Update(Encoder_t *encoder)
{
    // 1. 读取当前计数值
    int32_t current = __HAL_TIM_GET_COUNTER(encoder->htim);

    // 2. 计算脉冲增量
    int32_t delta = current - encoder->Last_counter;

    // 3. 16位计数器溢出处理
    if (encoder->htim->Instance == TIM3 || encoder->htim->Instance == TIM4)
    {
        if (delta > 32767)       delta -= 65536;
        else if (delta < -32768) delta += 65536;
        delta = -delta;  // 左轮方向反转
    }

    // 4. 更新累计脉冲和瞬时速度
    encoder->Total_counter += delta;
    encoder->Velocity = delta * VELOCITY_COEFFICIENT;
}
```

---

### 2.3 PID 控制算法

#### 2.3.1 增量式 PID

位置式 PID 输出为绝对值，增量式 PID 输出为变化量：

```c
typedef struct {
    float Kp, Ki, Kd;
    float Target, Measured, Error;
    float PrevError, PrevPrevError;
    float Output;
} PID_Controller_t;

float PID_Calculate(PID_Controller_t *pid, float target, float measured)
{
    pid->Target = target;
    pid->Measured = measured;
    pid->Error = target - measured;

    // 增量计算
    float p_term = pid->Kp * (pid->Error - pid->PrevError);
    float i_term = pid->Ki * pid->Error;
    float d_term = pid->Kd * (pid->Error - 2*pid->PrevError + pid->PrevPrevError);

    pid->Output += p_term + i_term + d_term;

    pid->PrevPrevError = pid->PrevError;
    pid->PrevError = pid->Error;

    return pid->Output;
}
```

#### 2.3.2 PID 参数调节

| 参数 | 作用 | 调参方法 |
|------|------|----------|
| Kp | 快速响应误差 | 增大 → 响应快、超调大 |
| Ki | 消除稳态误差 | 增大 → 稳态精度高、可能振荡 |
| Kd | 阻尼预测 | 增大 → 抑制振荡、提高稳定性 |

常用整定方法：Ziegler-Nichols 法或试凑法。

---

### 2.4 定时器中断

#### 2.4.1 中断频率确定

PID 控制需要固定采样周期，本系统使用 TIM6 产生 10ms 定时中断（100Hz）：

```c
// TIM6 配置
// PSC = 8399, ARR = 99
// 中断周期 = (8399+1) × (99+1) / 84MHz = 10ms

void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM6)
    {
        Encoders_All_Update();    // 更新编码器
        Motor_PID_Update_All();   // PID 计算
        Yaw_PID_Update();        // 转向环
    }
}
```

#### 2.4.2 中断优先级

| 中断 | 优先级 | 功能 |
|------|--------|------|
| TIM6 | 高 | PID 控制（实时性要求高） |
| USART DMA | 中 | 数据接收 |
| 其他 | 低 | 耗时操作 |

---

### 2.5 运动控制模型

#### 2.5.1 麦克纳姆轮运动学

四轮麦克纳姆轮底盘可实现全向移动：

```c
void Car_Move(int speed_left, int speed_right)
{
    MotorB_SetSpeed(speed_left);   // 左前
    MotorC_SetSpeed(speed_left);   // 左后
    MotorA_SetSpeed(speed_right);  // 右前
    MotorD_SetSpeed(speed_right);  // 右后
}

void Car_Go(int speed)    { Car_Move(-speed, -speed); }
void Car_Back(int speed)   { Car_Move(speed, speed); }
void Car_TurnLeft(int speed)  { Car_Move(speed, -speed); }
void Car_TurnRight(int speed) { Car_Move(-speed, speed); }
```

#### 2.5.2 差速转向

```c
// 左转：左轮后退，右轮前进 → 绕车身中心旋转
// 右转：左轮前进，右轮后退
```

---

### 2.6 硬件资源分配

| 电机 | PWM TIM | 编码器 TIM | DIR 控制 |
|------|---------|------------|----------|
| 右前轮 A | TIM10/11 | TIM2 | GPIOC |
| 左前轮 B | TIM9 | TIM3 | GPIOC |
| 左后轮 C | TIM1 CH1/2 | TIM4 | GPIOC |
| 右后轮 D | TIM1 CH3/4 | TIM5 | GPIOC |

---

### 2.7 常见问题分析

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 电机抖动 | Kp 过大或无滤波 | 降低 Kp，增加软件滤波 |
| 停车过冲 | Kd 不足 | 增加 Kd 或使用斜坡减速 |
| 转向跑偏 | 左右轮机械差异 | 独立调试 PID 参数 |
| 编码器跳变 | EMI 干扰或接线不良 | 屏蔽线，加滤波算法 |

---

*文档更新时间：2026-03-08*
