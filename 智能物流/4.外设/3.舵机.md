# 往年基础示例

> **⚠️ 重要提示**
> 以下内容基于 2025 年参赛作品的失败代码整理而成，仅作为**教学讲解作用**。
> 代码中存在诸多问题，请批判性学习！

---

## 3. 舵机

本节阐述智能物流小车中舵机驱动系统的设计与实现，包括 PWM 舵机与串口总线舵机的控制原理、通信协议及驱动代码。

---

### 3.1 舵机基本原理

#### 3.1.1 舵机内部结构

舵机是集成式位置伺服系统，内部包含：

- **直流电机**：执行元件
- **减速齿轮**：降低转速、增加扭矩
- **电位器**：角度反馈（构成位置闭环）
- **控制电路**：比较目标位置与当前位置，驱动电机运转

```
目标角度 → 比较器 → 驱动电路 → 电机 → 减速 → 输出轴
                              ↑
                        电位器反馈
```

#### 3.1.2 PWM 舵机控制原理

PWM 舵机通过 PWM 信号的高电平脉宽确定目标位置：

| 脉宽 | 角度 |
|------|------|
| 0.5ms | 0° |
| 1.0ms | 45° |
| 1.5ms | 90° |
| 2.0ms | 180° |

**数学关系**：
```
脉宽(μs) = 500 + 角度 × (2000/180)
```

---

### 3.2 PWM 舵机驱动

#### 3.2.1 定时器配置

```c
// TIM12 配置为 50Hz PWM 输出
// PSC = 83, ARR = 19999
// 周期 = 84 × 20000 / 84MHz = 20ms (50Hz)

static void MX_TIM12_Init(void)
{
    htim12.Instance = TIM12;
    htim12.Init.Prescaler = 83;
    htim12.Init.Period = 19999;
    htim12.Init.CounterMode = TIM_COUNTERMODE_UP;
    HAL_TIM_PWM_Init(&htim12);
}
```

#### 3.2.2 角度输出

```c
void Datou_PWM_SetAngle(uint8_t angle)
{
    // 脉宽计算：500-2500μs 对应 0-180°
    uint16_t pulse_us = 500 + (angle * 2000 / 180);

    // 转换为定时器比较值
    // 定时器频率 = 84MHz / 84 = 1MHz
    uint16_t cmp = pulse_us;  // 1MHz → 1μs 精度

    __HAL_TIM_SET_COMPARE(&htim12, TIM_CHANNEL_1, cmp);
}
```

---

### 3.3 串口总线舵机

#### 3.3.1 通信协议

Fashion Star 串口舵机采用自定义二进制协议：

```
┌──────┬────┬─────┬──────┬─────────────────┬────────┐
│ 包头  │ ID │ 长度 │ 指令 │ 数据            │ 校验和 │
│0xFF  │ xx │  n   │ 0x03 │ ...             │  ~Σ   │
└──────┴────┴─────┴──────┴─────────────────┴────────┘
```

**指令类型**：
| 指令 | 功能 |
|------|------|
| 0x01 | 角度查询 |
| 0x03 | 位置写入 |
| 0x04 | 速度写入 |
| 0x06 | 角度速度写入 |

#### 3.3.2 位置控制指令

```c
void Servo_SendPositionCommand(uint8_t id, uint16_t position)
{
    uint8_t command[13];

    // 帧头
    command[0] = 0xFF;
    command[1] = 0xFF;

    // 地址与控制
    command[2] = id;        // 舵机 ID
    command[3] = 0x09;     // 数据长度 9
    command[4] = 0x03;     // 指令：WRITE
    command[5] = 0x2A;     // 地址：目标位置寄存器

    // 数据
    command[6] = (uint8_t)(position & 0xFF);
    command[7] = (uint8_t)((position >> 8) & 0xFF);
    command[8] = 0x00;     // 时间低字节
    command[9] = 0x00;     // 时间高字节
    command[10] = 0xC4;    // 速度低字节
    command[11] = 0x09;    // 速度高字节

    // 校验和：ID + Length + Instruction + Address + Data 的和取反
    uint8_t checksum = 0;
    for (int i = 2; i < 12; i++) checksum += command[i];
    command[12] = ~checksum;

    HAL_UART_Transmit(&huart5, command, 13, HAL_MAX_DELAY);
}
```

#### 3.3.3 带加速度的速度控制

```c
void Servo_SendPositionAccelSpeedCommand(
    UART_HandleTypeDef *huart,
    uint8_t id,
    uint16_t acceleration,
    int position,
    uint16_t speed)
{
    uint8_t command[14];

    command[0] = 0xFF; command[1] = 0xFF;
    command[2] = id;
    command[3] = 0x0A;     // 数据长度 10
    command[4] = 0x03;     // WRITE
    command[5] = 0x29;     // 地址：加速度寄存器

    command[6] = acceleration;
    command[7] = (uint8_t)(position & 0xFF);
    command[8] = (uint8_t)((position >> 8) & 0xFF);
    command[9] = 0x00; command[10] = 0x00;
    command[11] = (uint8_t)(speed & 0xFF);
    command[12] = (uint8_t)((speed >> 8) & 0xFF);

    uint8_t checksum = 0;
    for (int i = 2; i < 13; i++) checksum += command[i];
    command[13] = ~checksum;

    HAL_UART_Transmit(huart, command, 14, HAL_MAX_DELAY);
}
```

---

### 3.4 环形缓冲区

#### 3.4.1 原理

串口数据逐字节到达，使用环形缓冲区实现异步数据缓存：

```
┌─────────────────────────────────────┐
│  buffer[0] ... buffer[head] ...   │
│                  ↑                 │
│                  tail              │
└─────────────────────────────────────┘

写入：head++ (到末尾则回绕)
读取：tail++ (到末尾则回绕)
```

#### 3.4.2 实现

```c
typedef struct {
    uint8_t *buffer;
    uint32_t size;
    uint32_t head, tail, count;
} RingBufferTypeDef;

void RingBuffer_WriteByte(RingBufferTypeDef *rb, uint8_t data)
{
    rb->buffer[rb->head++] = data;
    if (rb->head >= rb->size) rb->head = 0;
    rb->count++;
}

uint8_t RingBuffer_ReadByte(RingBufferTypeDef *rb)
{
    uint8_t data = rb->buffer[rb->tail++];
    if (rb->tail >= rb->size) rb->tail = 0;
    rb->count--;
    return data;
}
```

---

### 3.5 参数速查

#### 3.5.1 PWM 舵机

| 参数 | 值 |
|------|-----|
| 频率 | 50Hz |
| 周期 | 20ms |
| 脉宽范围 | 0.5-2.5ms |
| 角度范围 | 0-180° |

#### 3.5.2 串口舵机

| 参数 | 值 |
|------|-----|
| 波特率 | 115200 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 |
| 位置范围 | 0-4095 (12位) |
| 速度范围 | 0-3400 (0.732rpm/位) |

---

### 3.6 常见问题分析

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 舵机抖动 | PWM 频率偏差或信号干扰 | 校准 50Hz，增加滤波电容 |
| 不转动 | 协议错误或供电不足 | 校验和重算，检查电源 |
| 角度误差 | 脉宽映射偏差 | 实际测量校准公式 |
| 通信丢帧 | DMA 缓冲区不足 | 增大缓冲区，使用 IDLE 中断 |

---

*文档更新时间：2026-03-08*
